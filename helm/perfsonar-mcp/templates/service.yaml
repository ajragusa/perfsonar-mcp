apiVersion: v1
kind: Service
metadata:
  name: {{ include "perfsonar-mcp.fullname" . }}
  labels:
    {{- include "perfsonar-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "perfsonar-mcp.selectorLabels" . | nindent 4 }}
